import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.houseofhighmaintenance.app',
  appName: 'houseofhighmaintenance',
  webDir: 'build'
};

export default config;
